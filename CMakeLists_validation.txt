# GPU Bridge Validation Test
add_executable(gpu_bridge_validation 
    gpu_bridge_validation.cpp
)

target_link_libraries(gpu_bridge_validation 
    PRIVATE ve_gfx
    PRIVATE ve_core
)

target_include_directories(gpu_bridge_validation 
    PRIVATE ${CMAKE_SOURCE_DIR}/src
    PRIVATE ${CMAKE_SOURCE_DIR}/src/gfx
)
